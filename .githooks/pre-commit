#!/bin/sh
# Repo hygiene + host golden tests before every commit.
"$(git rev-parse --show-toplevel)/tools/check-repo-hygiene.sh" || exit 1
"$(git rev-parse --show-toplevel)/tests/run.sh" >/dev/null 2>&1 || {
    echo "golden tests failed — run tests/run.sh"; exit 1; }
exit 0
